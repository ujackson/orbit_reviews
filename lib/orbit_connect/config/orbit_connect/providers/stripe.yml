id: stripe
name: Stripe
description: Payments integration
category: development
auth_type: api_key
icon: Payments
color: "#635BFF"
enabled: true
provider_class: OrbitConnect::Providers::Stripe
docs_slug: stripe
setup_fields:
  - name: secret_key
    label: Secret Key
    type: password
